

/*const InfoRow = ({label, value, special = false}) => (
    <View style={Styles.infoRow}>
        <Text style={styles.label}>{label}</Text>
        <Text style={[stiles.value, special && styles.specialValue]}>{value}</Text>
    </View>
);*/

